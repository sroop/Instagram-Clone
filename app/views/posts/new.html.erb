<%= form_for @post, html: { multipart: true } do |f| %>
	

	<%= f.label :picture %>
	<%= f.file_field :picture %>

	<%= f.label :caption %>
	<%= f.text_field :caption %>

	<%= f.submit 'Add' %>

 <% end %>